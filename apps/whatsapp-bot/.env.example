# URL de la API (apps/api). En producción, la URL pública del servidor.
API_URL=http://localhost:4000

# Opcional: si se completa, el bot usa Claude para responder preguntas
# libres (que no matchean el menú) basándose SOLO en los datos del
# negocio. Sin esto, esas preguntas reciben el menú de nuevo.
ANTHROPIC_API_KEY=
